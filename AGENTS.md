# AGENTS.md

本文件适用于仓库根目录及全部子目录，供参与 Saber 开发的 AI 代理和工程师使用。交流、代码注释、提交说明默认使用中文；代码标识符保持英文。

## 1. 项目定位与事实来源

Saber 是 SpringBlade 的官方 Vue 3 管理端，与 SpringBlade Boot/Cloud 后端配套。当前仓库版本为 `5.0.1`，
实际依赖以 `package.json` 和锁文件为准。项目已进入业务页面全面迁移 Element Plus 的阶段：新增页面和纳入迁移
范围的页面以 Vue 3.5、TypeScript、Vite 5、Element Plus、Vue Router 4、Vuex 4、Axios、vue-i18n 和 Sass
为目标技术栈。Avue 及其插件仅作为尚未迁移页面的临时兼容依赖，不再作为新开发或重构页面的实现方案。

遇到文档、模板和现有代码不一致时，按以下优先级判断：

1. 用户当前需求、已确认的需求文档和验收标准。
2. `package.json`、`vite.config.mts`、`tsconfig*.json`、`.env.*` 等当前配置。
3. 目标模块当前 API、后端契约和现有业务行为。
4. 已完成迁移的同类型 Element Plus 页面、公共组件和 composable。
5. `README.md` 中的项目背景信息。

旧版 Avue 页面仅用于确认迁移前字段、接口和交互，不是目标代码模板。旧实现与已确认需求冲突时，以需求和后端
契约为准；旧实现存在但文档未说明的行为必须记录并确认，不得在迁移中静默删除或机械复制。

## 2. 工程结构

```text
src/
├── api/                 # 按业务模块组织的 HTTP 接口
├── components/          # Element Plus 公共组件及少量全局基础容器
├── composables/         # 分页、树列表、远程详情/选项、权限和选择状态
├── config/              # website、环境地址、图标等全局配置
├── lang/                # zh/en/ja 国际化资源
├── mixins/              # 尚未迁移页面的历史兼容代码，不新增能力
├── option/              # 尚未迁移页面的历史 Avue 配置，不新增文件
├── page/                # 登录、锁屏、主布局，仍允许保留 Options API
├── router/              # 静态路由、动态菜单路由和组件装配
├── store/               # Vuex store、getters 与业务模块
├── styles/              # 全局样式、变量、mixin 和明暗主题
├── types/               # 项目自建的最小类型声明
├── utils/               # 鉴权、加密、校验、存储和通用工具
├── views/               # 业务页面；必须使用 Composition API + TypeScript
├── axios.ts             # Axios 实例、鉴权头和统一错误处理
├── permission.ts        # 登录、锁屏、标签页等路由守卫
└── main.ts              # 应用入口和全局注册
vite/                    # Vite 插件；现有插件文件刻意保留 JavaScript
doc/                     # 需求、设计、测试、模板与迁移指南
```

路径别名：`@` 指向 `src`，`~` 指向仓库根目录，`components`、`styles`、`utils` 分别指向对应的 `src` 子目录。优先使用别名，避免深层相对路径。

## 3. 开始开发前

1. 先阅读目标业务的 `src/views/{module}/`、`src/api/{module}/`、关联需求和详细设计，再确认迁移前字段、接口、权限码和异常分支。
2. 分页 CRUD 优先参考已迁移的 `src/views/system/param.vue`、`post.vue`；树表、复杂表单和授权树分别参考 `dict.vue`、`user.vue`、`authority/role.vue`；工具页面参考 `src/views/tool/code.vue`。
3. 搜索 `src/components/`、`src/composables/`、`src/types/`、`src/utils/` 和现有 API，优先复用已有 Element Plus 能力。
4. 明确当前任务涉及的迁移页面和回归页面。一个页面一旦纳入迁移范围，应完整移除 Avue CRUD/Form，不保留长期混用状态。
5. 检查工作区状态，保留用户已有改动；不要顺手格式化、迁移或重构范围外文件。

## 4. Vue 与 TypeScript 规范

### 4.1 页面范式

- `src/views/**/*.vue` 新增和修改统一使用 `<script setup lang="ts">` 与 Composition API。
- 禁止在业务页面新增 `data()`、`methods`、`computed` 选项、`mapGetters` 或 `this.$xxx`。
- `src/components/`、`src/page/` 仍处于渐进迁移状态。范围外局部修改遵循文件现有范式；纳入迁移任务时再完整迁移。
- 新增 API、工具、配置、store、router、lang、component 和 composable 文件使用 `.ts`。不新增 `src/option/` 或 CRUD mixin 文件。
- `src/mixins/crud.js`、现有 `src/option/` 和 `vite/plugins/*.js` 是历史 JavaScript；除迁移删除或阻断性修复外不扩展。
- 方法和事件处理器使用 `const fn = (...) => {}`。

### 4.2 导入和全局能力

- Vue/Vuex API 必须显式导入且只导入实际使用项，如 `ref`、`reactive`、`computed`、`watch`、`useStore`。虽然工程配置了 auto-import，但没有生成声明文件。
- 消息与确认框使用 `ElMessage`、`ElMessageBox` 显式导入。
- 脚本中使用 `import website from '@/config/website'`、`import dayjs from 'dayjs'`、`import request from '@/axios'`，不要依赖 `this.website`、`this.$dayjs` 或 `window.axios`。
- 权限判断优先复用 `useCrudPermission`；其他现有能力使用 `validData` 时从 `@/utils/util` 导入。
- 空值校验从 `@/utils/validate` 导入 `validateNull`，不要复制一套相似校验函数。

### 4.3 状态与类型

- 实体使用局部 `interface XxxEntity`，表单通常为 `type XxxForm = Partial<XxxEntity>`。
- 列表使用 `ref<XxxEntity[]>([])`；表单、查询、选中项和加载状态使用 `ref`。
- 分页状态优先由 `usePagedList` 管理，非分页树状态优先由 `useTreeList` 管理；不要在页面重复实现请求序号、loading 和失败恢复。
- Element Plus 实例使用公开类型或 `ref<InstanceType<typeof ElXxx>>()`；表单提交前必须通过表单实例的 `validate()`。
- 菜单项等已有公共模型优先复用 `@/types/menu`，不为单页字段建立跨模块巨型实体类型。
- 列表响应、分页、树节点和远程选项优先复用 `@/types/list`、`@/types/tree` 和 `@/types/option` 中的契约。
- 禁止显式 `any`，也不要用裸 `unknown` 或 `Record<string, unknown>` 掩盖未梳理的数据结构。类型检查未要求时避免过度标注；确实不读取内容的对象参数可用 `object`。
- 工程当前 `strict: false`、`allowJs: true`、`checkJs: false`；不要擅自一次性开启严格模式。

### 4.4 格式与命名

遵循 `.prettierrc.json`：100 字符行宽、2 空格、分号、单引号、箭头函数单参数不加括号、对象括号保留空格。不要对未修改文件做全仓格式化。

- 页面文件：kebab-case，如 `api-scope.vue`。
- 公共组件：kebab-case 目录配 `main.vue`。
- API、composable、类型和工具文件：camelCase。
- 变量和函数：camelCase。
- Vuex mutation：UPPER_SNAKE_CASE；action 沿用现有 PascalCase。
- 权限码：`{module}_{action}`，如 `dict_add`、`user_delete`。

## 5. API 层约定

- 所有请求经 `@/axios` 发起，不另建 Axios 实例，也不要重复实现 Token、NProgress 或统一错误提示。
- Blade 接口统一使用 `/blade-{module}/...` 前缀；Boot 和 Cloud 前端路径一致。
- 标准命名：`getList(current, size, params)`、`getDetail(id)`/`getXxx(id)`、`add(row)`、`update(row)`、`remove(ids)`、`getXxxTree()`。
- 分页接口通常返回 `res.data.data.records` 和 `res.data.data.total`；树或非分页接口可能直接返回 `res.data.data`。以接口和相邻页面为准。
- GET 查询放 `params`，POST 请求体放 `data`。批量删除的 `ids` 通常为逗号分隔字符串。
- 新增和修改通常共用 `/submit`，但已有模块存在专用 `/update` 时必须遵循实际后端契约，不能机械改写。
- 需要加密令牌时使用请求配置 `cryptoToken: true`；不要自行拼装 `Blade-Auth`。项目的固定 Cookie Key、OAuth Basic 头和 SM2 登录加密不得随意改变。
- 不要在页面重复展示由 `axios.ts` 已统一处理的普通接口错误；失败路径仍必须恢复页面 loading、提交锁和可重试状态。

## 6. Element Plus 页面约定

### 6.1 页面骨架与复用

- 业务页面外层沿用 `<basic-container>`，使用 Element Plus 组件和现有公共组件组合页面，不新建声明式 CRUD 引擎。
- 搜索区优先使用 `SearchPanel`，列表区使用 `ListPanel`，分页使用 `ListPagination`，表单弹窗使用 `FormDialog`，详情抽屉使用 `DetailDrawer`，行操作使用 `RowActions`。
- 分页列表优先复用 `usePagedList`，树列表使用 `useTreeList`，详情和动态选项分别使用 `useRemoteDetail`、`useRemoteOptions`，表格选择使用 `useTableSelection`。
- 只在职责、交互和数据契约稳定且有多个真实使用方时抽取公共组件或 composable；业务字段、API 参数和模块规则保留在页面或模块内。
- 页面应保持搜索、工具栏、表格、分页、弹窗的稳定布局。动态内容、loading、空数据和错误提示不得改变固定工具区尺寸或遮挡主要操作。

### 6.2 列表、表单与异常状态

- 查询时仅提交白名单字段并将页码归 1；重置时清空查询和页码；刷新保留最后一次已执行查询。每次用户操作只产生一次有效列表请求。
- 分页接口以服务端 `records` 和 `total` 为准；树接口不伪造分页。树表设置稳定 `row-key="id"`，按后端 `children` 展示层级。
- 加载、详情、选项和提交使用独立状态。快速切换查询、记录或租户时，旧响应不得覆盖当前上下文。
- 新增、查看和编辑共用表单时必须显式重置 mode、form、校验、只读状态和远程选项；关闭后不得把上一条详情或新增子项上下文带入下一次操作。
- 新增子项应显式保存父级上下文并写入 `parentId` 或实际外键；普通新增必须清除该上下文。编辑节点时不得允许选择自身或后代形成循环。
- 保存成功后关闭弹窗并按业务需要刷新；失败时保留用户输入、恢复提交按钮且不显示成功。详情或选项失败时清空旧值，并禁止提交依赖未成功加载的数据。
- 单删和批量删除均需确认；空选择不发请求。成功后刷新并清空选择，失败时保留记录、查询、页码和可重试选择。
- 主子表或复杂配置优先使用抽屉或对话框承载子列表，主表和子表分别维护 form、data、loading、page 和选择状态；提交子项前显式写入主记录 ID。

### 6.3 字典、权限与专项操作

- 字典显示与选择优先使用 `useDictionary`、`DictSelect` 和 `DictTag`；远程字典必须显式映射 label/value，保留后端约定的字符串或数值类型，不做真假值隐式转换。
- 标准按钮权限使用 `useCrudPermission` 或 `store.getters.permission`，权限码保持 `{module}_{action}`。自定义入口同时执行可见性和操作前校验；后端仍是最终安全边界。
- 超级管理员专项入口沿用 `userInfo.authority.includes('admin')`，不得把管理员身份等同于普通按钮权限。
- 授权树优先使用 `TreeCheckPanel`；树数据、已选 keys、联动、半选和提交状态必须相互隔离，任一加载失败时不得展示不完整授权为当前结果。
- 上传使用 Element Plus Upload 和现有 Axios 请求；下载使用 Axios Blob 与标准认证头。不得把 Token 放入 URL，也不得把文件内容、密码或授权 keys 写入日志。
- 内联排序、授权、重置、解锁、复制、导入导出和代码生成等专项操作必须有独立执行锁、明确确认、成功判定和失败恢复；有服务端副作用时不得宣称前端已自动回滚。

### 6.4 Avue 迁移边界

- 新增页面和纳入迁移范围的页面禁止使用 `avue-crud`、`avue-form`、Avue option/dicUrl、`crud.js` mixin 或 Avue 保存回调。
- 不新增 `src/option/` 文件，不为迁移页面扩展 Avue 全局注册、插件或样式。旧页面只能用于核对业务契约。
- 尚未迁移的页面可以暂时运行，但除阻断性兼容修复外不继续增加业务能力；相关需求应先将该页面整体迁移为 Element Plus。
- 单页迁移必须保持菜单入口、API、权限码、租户和业务结果一致，并完成静态检索，确保目标页无 Avue CRUD/Form 引用和 `.avue-*` 定向样式。
- `src/router/avue-router.ts` 的文件名属于历史命名且承载动态路由装配，不因页面迁移擅自重命名。全量迁移完成后，移除 Avue 依赖、全局注册、遗留 mixin/option 和样式应作为独立清理任务执行完整回归。

## 7. 路由、权限、租户与国际化

- 静态页面路由位于 `src/router/page/` 和 `src/router/views/`；业务菜单主要由后端 `/blade-system/menu/routes` 动态下发。
- 动态组件遵循 `views/{path}.vue` 约定，由 `src/router/avue-router.ts` 的 `import.meta.glob` 装配。新增普通业务页面通常不需要手写前端路由。
- 外链菜单由路由层转换为 iframe 或新窗口，并可能替换 `${token}`；修改此链路时同时检查鉴权和 URL 编码。
- 按钮权限来自 `store.getters.permission`。不要仅隐藏按钮而遗漏接口侧/后端权限，前端显示控制不是安全边界。
- 多租户开关来自 `website.tenantMode`；不要硬编码租户模式或管理租户编号。
- 新增菜单 i18n key 时同步维护 `src/lang/zh.ts`、`en.ts`、`ja.ts` 的 `route.*` 条目。

## 8. 样式、响应式与可访问性

- 优先使用 Element Plus 和现有公共组件，不重复封装相同能力，不引入第二套 UI 组件库。
- 全局 SCSS 变量和 mixin 位于 `src/styles/variables.scss`、`mixin.scss`；优先使用现有变量，避免硬编码主题色。
- 明暗主题与动态主色统一位于 `src/styles/theme/tokens.scss`，调整时必须同时检查浅色、深色和自定义主色。
- 页面布局保持后台系统的紧凑、可扫描风格；不要引入与现有系统不一致的营销式布局或大面积装饰。
- Element Plus 图标已全量注册。沿用项目现有图标命名方式，不手写重复 SVG。
- 表格、工具栏、分页、弹窗和树控件必须定义稳定的响应式约束；至少检查 1440px、1024px 和 375px，移动端允许换行或横向滚动，但主要操作必须可达。
- 表单弹窗保持 header/footer 可见，正文独立滚动；焦点顺序、键盘关闭/确认、危险操作辨识和 loading 对比度应可用。
- 不新增 `.ant-*`、`.avue-*` 或针对第三方内部 DOM 的大范围 `:deep()` 补丁；通用视觉问题应在共享组件或主题 Token 层解决。

## 9. 文档契约

### 9.1 文档体系与模板

- `doc/requirements-index.md` 是需求编号、版本、状态和关联文档的统一登记入口，需求正文和详细变更记录保存在对应文档中。
- 需求文档使用 `doc/templates/requirements-template.md`，详细设计使用 `doc/templates/detailed-design-template.md`，涉及数据库变化时使用 `doc/templates/database-design-template.md`，测试文档使用 `doc/templates/test-document-template.md`。
- 新文档沿用 `REQ-YYYY-NNN` 编号：需求为 `doc/requirements/REQ-YYYY-NNN-short-name.md`，设计为 `doc/design/DESIGN-REQ-YYYY-NNN-short-name.md`，数据库设计文件名为 `DB-REQ-YYYY-NNN-short-name.md`，测试为 `doc/test/TEST-REQ-YYYY-NNN-short-name.md`。
- 文档、模板和实现不一致时按第 1 节事实来源处理。模板用于保证信息完整，不得用模板占位符覆盖已经确认的业务事实。

### 9.2 需求驱动任务的文档同步

需求讨论、设计、开发、测试和验收是同一条文档链，状态发生变化时统一维护，不得只修改代码或其中一份文档：

1. 需求确定后先在索引分配编号，再按“需求文档 -> 数据库设计（按需） -> 详细设计 -> 测试文档”的顺序建立关联文档。
2. 不涉及数据库变化时在需求、设计和索引中写明“不涉及”，不要创建空数据库设计。
3. 开发实现完成或进入下一阶段时，同步更新需求文档、详细设计和索引的版本、状态、最后更新日期、关联链接和变更记录；涉及数据库时同步数据库设计。
4. 需求和设计必须记录实际完成范围、实现偏差、验证命令与结果、未完成验收项和已知环境限制，文档描述必须与代码现状一致。
5. 文档版本从 `0.1` 开始；评审确认后使用 `1.0`，确认后的兼容变化递增次版本，重大范围变化递增主版本。每次版本变化都要写变更记录。
6. 状态使用索引定义的统一枚举。仅完成开发或部分验证时保持“开发中”，正文可注明“开发完成，待验收”；只有全部验收标准和必要业务回归通过后才能标记“已验收”。
7. 文档新增或关联关系变化后，检查需求、设计、数据库设计、测试文档和索引之间的相对链接，避免单向关联或失效路径。

### 9.3 测试文档编写

需求进入“开发完成，待验收”、用户明确要求编写测试文档，或详细设计将测试文档列为待办时，必须在
`doc/test/` 创建或更新关联测试文档。不得脱离模板另建只有用例列表或测试结论的简化文档。

1. 测试事实来源依次为用户当前要求、关联需求、详细设计、当前代码与 API 契约、同阶段既有测试文档。发现不一致时记录待澄清项，不得自行选择有利于通过的预期。
2. 保留模板中的文档信息、测试范围、测试环境、测试用例、缺陷记录、测试汇总、测试结论和变更记录。可以增加使用说明、测试准备、覆盖矩阵、数据清理和执行约束，但不得删除模板核心信息。
3. 文档信息至少包含测试编号、关联需求、关联设计、负责人、状态、测试日期、版本、创建日期、最后更新日期及前后端版本或提交。未知信息填写“待准备”或“待执行”，不得猜测。
4. 测试范围分别写明目标、范围内页面/接口/流程和不测试内容。后端、数据库、外部服务或人工核验不在范围时必须明确标注。
5. 从需求逐条提取验收标准、业务规则、异常流程、安全约束、权限/租户边界和回归要求。文末建立验收标准覆盖矩阵，保证每项验收标准至少关联一个可执行用例；存在缺口时记录阻塞原因。
6. 每个用例必须是可独立验收的完整状态链，至少包含唯一编号和名称、优先级、关联验收标准、模块与页面入口、前置账号角色/权限/租户/数据、依赖、完整步骤、逐步可观察预期、证据要求、清理要求、实际结果、结果状态和缺陷引用。
7. 用例还应声明是否可并行及理由、是否涉及共享登录态或全局配置、建议浏览器能力与执行面、跨轮连续性要求。端口、批次、模型和实际并发数由执行阶段根据运行时能力生成，不在编写阶段虚构。
8. 步骤必须使用明确的页面入口、按钮名称、输入数据和观察点；预期同时覆盖可见页面状态及影响结论的控制台或网络状态，不写“功能正常”“显示正确”等笼统表述。
9. 写操作用例定义唯一数据前缀和精确清理范围。删除、授权、租户、导入、下载、代码生成等高风险行为必须写清允许操作的数据、确认点、副作用和失败恢复；无法安全清理的数据不得授权自动删除。
10. 测试文档、截图、日志和缺陷记录不得包含密码、Token、客户端密钥、数据源凭据、上传文件正文或其他敏感数据。账号只记录代号、角色和最低权限，实际凭据由执行环境安全提供。
11. 编写过程中可使用“草稿”，完成自检后改为“待执行”；用例结果初始为“未执行”。正式结果只使用“未执行”“通过”“失败”“阻塞”或“不适用”，汇总数量必须与实际用例数一致。
12. 建档或测试范围、版本、状态变化后，按第 9.2 节同步关联需求、设计和索引；仅完成测试文档编写不得把需求标记为“已验收”。

测试文档完成后至少检查：链接可达；用例编号唯一；用例总数与汇总一致；全部验收标准均已映射；前置条件、
步骤、预期、证据和清理要求完整；没有敏感信息；状态与实际执行进度一致。

### 9.4 测试执行结果归档

- 测试完成或因明确阻塞阶段性收尾时，在测试文档记录发现用例总数、执行范围、各状态数量、逐用例结果、证据位置、缺陷、数据清理和环境限制。
- 同步更新需求、详细设计和索引的版本、状态、最后更新日期与变更记录；涉及数据库设计但本次未验证后端时，明确后端不在测试范围。
- 记录实际使用的代理模型、推理强度、服务层级、并发数、批次、端口范围、浏览器能力、执行面和跨轮交接方式，不得记录未实际设置的参数。
- 记录未执行、阻塞、不适用、重试后不稳定和仍需真实业务环境验证的内容。存在失败、阻塞或未执行用例时，需求保持“开发完成，待验收”或原有未完成状态。
- 归档后检查实际 diff，确保用例数和汇总一致，没有覆盖用户已有改动，没有写入账号密码、Token、本机专用后端地址或临时调试信息。

## 10. 命令与验证

使用 `pnpm`，不要生成 npm/yarn 锁文件。Node.js 要求 18 或更高版本。

```bash
pnpm run dev          # 开发服务器，默认 http://localhost:2888，监听 host
pnpm run prod         # 以 production 模式启动 Vite 开发服务
pnpm run type-check   # vue-tsc + Node 侧 tsc；不生成文件
pnpm run build        # 默认模式构建
pnpm run build:prod   # production 模式构建到 dist/
pnpm run serve        # 预览构建产物
```

`pnpm run build` 只由 Vite/esbuild 转译，不包含类型检查。改动 `.ts` 或 `.vue` 后至少运行
`pnpm run type-check`；完成页面迁移，或影响构建、依赖、环境变量、Vite 配置时再运行 `pnpm run build:prod`。

Element Plus 页面迁移还必须执行与范围匹配的检查：

- 对目标文件静态检索 `avue-crud`、`avue-form`、Avue import、option/mixin 和 `.avue-*` 样式引用。
- 启动开发服务验证目标菜单、页面主流程、控制台和网络请求，并检查浅色/深色及 1440px、1024px、375px 视口。
- 回归受共享组件或 composable 影响的已迁移页面；尚未完全移除 Avue 前，再选择至少一个未迁移页面确认兼容性。
- 只将实际执行且取得结果的检查写入验证记录；静态扫描、类型检查、构建和浏览器验收不可互相替代。

当前仓库没有单元测试、端到端测试或 lint 脚本。不要虚构命令；若新增测试基础设施，必须是用户需求的一部分并说明范围。

开发环境的 `VITE_APP_API=/api`，Vite 将 `/api` 代理到 `VITE_APP_PROXY_TARGET`（默认 `http://localhost`）。端口由 `VITE_APP_PORT` 控制，默认 `2888`。不要提交本机专用后端地址或密钥。

## 11. 变更与提交纪律

- 保持改动最小且聚焦，不修改无关格式、生成物和依赖元数据。
- `dist/` 是构建产物，除非用户明确要求发布产物，否则不要手工编辑或提交其变化。
- 引入新依赖前确认现有 Vue、Element Plus、dayjs、Axios、crypto-js、sm-crypto、NProgress 等能力不能满足需求，并说明新增依赖的必要性。
- 不修改认证头、Token Cookie Key、SM2 公钥、多租户行为或动态路由契约，除非需求明确要求且已检查后端兼容性。
- 提交前查看实际 diff，运行与风险匹配的验证；不要覆盖用户未提交的改动。
- Git 提交采用 Gitmoji 代码加中文描述，例如 `:sparkles:` 功能、`:bug:` 修复、`:zap:` 性能、`:lipstick:` 样式、`:recycle:` 重构、`:wrench:` 配置、`:memo:` 文档、`:fire:` 删除。只在用户明确要求时创建提交。

## 12. 完成标准

交付前确认：实现与相邻模块风格一致；权限、加载、空数据、错误和分页状态完整；API 参数位置和返回结构正确；
TypeScript 门禁通过；没有无关变更、调试日志、临时地址或敏感信息。需求驱动任务同时按第 9 节完成文档同步。

最终说明改动文件、行为变化、已运行的验证，以及仍需在真实后端环境验证的鉴权、多租户、文件或业务流程。
未执行的验证必须明确说明，不得用类型检查或静态检索替代业务验收。

## 13. 测试文档驱动的多代理前端测试执行

### 13.1 角色与边界

本节及后续小节适用于用户要求依据测试文档执行 Saber 前端测试，并允许主代理将测试案例分派给子代理的任务。
测试案例数量、顺序和验收范围必须来自本次指定的测试文档，不得预设为固定数量，也不得以示例中的案例数代替
实际解析结果。

- 主代理是测试编排者、质量门禁和唯一的公共文档写入者。主代理的第一项工作必须是解析测试文档并形成任务划分清单；完成划分前不得启动子代理执行案例。
- 子代理是单案例执行者。默认一个完整、可独立验收的测试案例对应一个子代理任务，不得把同一业务流程中必须共享页面状态的步骤拆给多个子代理。
- 主代理不占用普通案例名额，主要负责基线检查、任务划分、端口和进程管理、批次调度、结果复核、失败归因、文档同步与环境清理。证据不足或结论冲突时，主代理可以亲自复现或安排复测。
- 测试范围仅为 Saber 前端。测试实例共用既有后端和数据库，不为每个案例额外部署后端；但前端操作产生的数据仍必须按案例隔离，避免并发覆盖。
- 测试任务发现缺陷后只记录问题，不自动修改业务代码。只有用户明确要求进入修复阶段时，才按开发任务实施代码变更和相应验证。
- 子代理不得修改业务代码、公共测试文档、需求文档、详细设计或 `doc/requirements-index.md`。这些文件只能由主代理在汇总阶段统一更新。

### 13.2 阶段一：确定测试事实来源

主代理按以下顺序确定本次测试范围：

1. 读取用户明确指定的测试文档、需求文档和详细设计。
2. 若用户只给出需求或模块名称，在 `doc/` 及关联需求目录中定位对应测试文档，并通过需求编号、模块名称和版本确认关联关系。
3. 若存在多个无法可靠区分的测试文档，或文档版本与当前代码目标不一致，先向用户确认，不得自行选择可能过期的版本。
4. 记录测试文档路径、文档版本、最后更新日期、关联需求、目标前端版本和本次测试日期。
5. 以文档中实际识别的案例总数作为 `discoveredCaseCount`，同时统计可执行、阻塞、重复和不在本次范围内的案例数量。不得只报告已成功分派的案例数。

主代理逐条提取以下信息；缺失时必须显式记录，不得由子代理猜测：

- 案例编号和名称。
- 所属模块、菜单路径和目标页面。
- 前置账号、角色、权限、租户及必要的前置数据。
- 操作步骤、输入数据和预期结果。
- 是否涉及登录、退出、权限切换、路由、主题、国际化、上传下载或 CRUD 操作。
- 与其他案例的依赖关系和共享状态。
- 验收证据要求、清理要求及文档中的特殊限制。

### 13.3 阶段二：案例完整性与可执行性检查

主代理在划分任务前为每个案例执行预检查：

- 案例必须具有可定位的页面入口、可执行步骤和可观察的预期结果。
- 需要特定账号、权限或测试数据时，必须确认相应条件可用；敏感凭据不得写入仓库文档、测试报告或日志。
- 文档步骤与当前界面名称不一致时，先结合目标模块现有实现判断是否为文档漂移；无法确定时标记“待澄清”。
- 完全重复的案例只保留一个执行任务，但必须在任务清单中记录被合并的案例编号和理由。
- 缺少关键前置条件、无法判断预期或超出本次前端范围的案例不分派执行，状态记为“阻塞”或“不在范围”，并记录原因。
- 同一案例中的登录、导航、编辑、提交和结果验证形成连续状态链时，必须作为一个原子任务交给同一子代理。

案例状态在执行前只能是：

- `READY`：信息完整，可以执行。
- `BLOCKED`：缺少账号、权限、数据、环境或明确预期。
- `OUT_OF_SCOPE`：不属于本次前端测试范围。
- `DUPLICATE`：与其他案例重复，已记录归并关系。

### 13.4 阶段三：任务划分清单

任务划分清单是主代理启动测试前的第一个正式交付物。每个 `READY` 案例必须包含：

```text
caseId: 案例编号
title: 案例名称
source: 测试文档路径及章节
module: 所属模块和页面入口
preconditions: 账号角色、权限、前置数据和初始页面状态
steps: 必须完整执行的步骤
expectedResults: 每个关键步骤对应的可观察结果
dependencies: 前置案例编号；无依赖时为空
parallelizable: 是否允许并行及判断理由
batch: 执行批次
port: 独立前端测试端口
dataPrefix: 唯一测试数据前缀
evidence: 截图、页面断言、控制台或网络信息要求
cleanup: 仅允许清理的本案例数据
browserSkill: browser:control-in-app-browser | playwright，按案例要求选择
browserSurface: auto | iab | chrome | headless，并注明选择理由
browserContinuity: same-turn | handoff | deliverable；默认 same-turn
model: gpt-5.6-luna
reasoningEffort: max
```

划分任务时遵循以下规则：

- 默认一案例一任务，不因案例步骤较少而擅自合并。
- 不把一个案例拆成多个子任务，除非测试文档本身将其定义为可独立验收的子案例。
- 有依赖关系的案例按拓扑顺序分批；依赖未通过时，后续案例标记阻塞，不得绕过前置条件制造通过结果。
- 会同时修改同一条数据、同一角色、菜单、租户、全局配置或同一登录态的案例不得并行。
- 登录、退出、切换账号和权限刷新类案例默认串行，因为 localhost 不同端口仍会共享 Cookie。
- 只读取页面、互不共享状态且数据独立的案例可以并行。
- 每个可能创建数据的案例使用唯一前缀，建议为 `{caseId}_{timestamp}_`；子代理只能清理带有本案例前缀且由其创建的数据。

### 13.5 阶段四：并发、模型和批次调度

- 主代理先读取当前运行时允许的代理并发上限，再计算 `workerCount = min(READY 案例数, 可用子代理槽位数)`。主代理本身占用一个运行槽位，不得按总槽位数启动同等数量的子代理。
- 在当前约定的四槽运行环境中，最多同时运行三个子代理；案例多于三个时按依赖关系和任务清单分批执行，每批最多三个。
- 子代理统一指定 `gpt-5.6-luna`，推理强度设置为 `max`。若当前子代理接口支持速度设置，则使用 `standard`；若接口不暴露速度参数，必须沿用平台实际服务层级并在最终报告中说明，禁止虚构已设置 `standard`。
- 无论子代理是否继承会话历史，主代理都必须在提示中完整写入案例任务、测试约束、仓库路径和适用规则。子代理不读取根目录 `AGENTS.md`，提示中也不得用“遵循仓库规则”等引用替代实际约束；若缺少影响结论的关键条件，子代理应立即按阻塞上报，由主代理补全任务契约。
- 可见浏览器案例统一使用当前会话提供的 `browser:control-in-app-browser`，允许无界面独立上下文且测试文档未要求可见浏览器时才可选择 `playwright`。不得指定旧版 `browser-use 0.1.0-alpha2`，也不得在提示中硬编码插件缓存版本或本机插件路径。只有任务所需的技能说明仍由子代理按平台要求读取。
- 测试文档未指定浏览器类型时，`browserSurface` 使用 `auto`，由当前 Browser 能力按目标 URL 选择可用浏览器；明确要求内置浏览器时才使用 `iab`，明确要求 Chrome 时才使用 `chrome`。选择 `playwright` 时将 `browserSurface` 记为 `headless`。
- `iab` 与 Codex 任务会话绑定，涉及 `iab` 的案例默认串行，不得假设主代理或其他子代理的 `iab` 实例可以共享。只读、数据独立且使用不同浏览器上下文的案例才允许并行。
- 主代理只在有空闲槽位、对应测试端口已就绪且全部依赖已满足时启动下一任务。
- 某个子代理完成后可以释放槽位并调度下一案例；不能为了追求并发跳过依赖或复核环节。

推荐批次算法：

1. 将所有 `READY` 案例按依赖关系生成有向无环图；发现循环依赖时暂停相关案例并修正文档问题。
2. 从无未完成依赖的案例中筛选可并行集合。
3. 排除登录态、全局配置和共享数据冲突后，按文档顺序选择不超过 `workerCount` 个案例组成当前批次。
4. 当前批次完成并通过主代理结果门禁后，再计算下一批，不在开始时假设后续依赖一定通过。

### 13.6 阶段五：测试端口和进程管理

- `2888` 是用户查看 Saber 的主要端口，测试期间必须保持可用。主代理和子代理不得占用、重启、停止或重新配置该端口上的进程。
- 测试端口从 `2889` 开始。按任务划分清单中的稳定案例序号分配：首个案例使用 `2889`，后续案例依次递增。案例调整批次时端口映射保持不变，便于追踪证据。
- 每个测试实例使用 `pnpm run dev --port <port> --strictPort` 启动。当前 `pnpm 10` 会把额外的 `--` 原样传给 Vite，因此不得使用 `pnpm run dev -- --port ...`。必须保留 `--strictPort`，并在启动后核对 Vite 日志和实际监听端口；若未监听计划端口，立即按已记录 PID 停止本次实例并判定启动失败。
- 分配前检查端口是否可用。计划端口被其他进程占用时，不得停止未知进程；应选择下一个可用测试端口，并同步更新任务清单。
- 只启动当前批次所需的前端实例，不一次性启动全部案例端口。批次结束并完成证据收集后，停止已完成案例的实例，再启动后续批次。
- 主代理记录每个测试实例的案例编号、端口、进程 PID、启动时间和日志位置。停止服务时只能按记录的 PID 精确停止，禁止批量结束全部 `node`、`pnpm` 或 Vite 进程。
- 每个测试端口启动后必须先检查页面可访问性，再分派子代理；启动失败应先由主代理处理，不得让子代理把服务不可用误报为案例失败。
- 测试开始前和全部结束后都要确认 `2888` 可访问。若 `2888` 在测试前已不可用，只记录基线状态，不擅自重启用户进程。
- 多个实例共享同一源码目录，测试期间子代理不得修改源码，以免热更新同时影响其他案例和用户查看页面。

### 13.7 阶段六：子代理任务契约

主代理发送给每个子代理的提示必须是自包含任务，至少包括：

- 仓库绝对路径，以及本案例适用的角色边界、只读/写入权限、禁止事项和安全约束。子代理无需且不应再次读取 `AGENTS.md`。
- 唯一的 `caseId`、测试文档位置及完整案例内容。
- 指定端口和完整访问 URL，禁止访问或操作 `2888`。
- 前置账号角色、允许使用的数据以及禁止操作的共享数据。
- 逐步操作、每步预期结果和最终验收条件。
- 必须检查的页面状态、控制台错误和必要的失败请求信息。
- 必须提交的截图、日志或其他证据及其存放规则。
- 浏览器技能、执行面和跨轮策略；浏览器技能只写稳定名称，不写插件缓存路径或版本号。
- 数据清理范围、禁止修改代码和禁止写公共文档的约束。
- 统一结果格式、超时限制和遇到阻塞时的上报方式。

子代理执行规则：

- 只执行分配的案例，不顺带测试、修复或评价其他案例。
- 严格按文档步骤操作；为完成同一意图而进行的无风险页面定位可以自主处理，但不得自行改变业务验收标准。
- 每次关键页面变化后检查实际状态。不能仅根据按钮点击成功或接口返回成功推断整个案例通过。
- 前端功能测试至少检查可见页面结果和控制台错误；接口失败影响页面结果时记录请求 URL、方法、状态码和响应摘要，但不得泄露 Token 或敏感数据。
- 浏览器页面断言、截图和控制台检查默认在同一子代理轮次内完成。确需跨轮继续时，子代理必须在返回前将标签页标记为 `handoff`；需要把页面留给用户查看时标记为 `deliverable`，不得依赖未标记的临时标签页。
- 遇到 `browser-client is not trusted` 时按浏览器插件环境阻塞上报，由主代理提示用户执行 `Debug Menu > Plugins > Reload bundled plugins` 后再安排一次复测；不得切换到旧插件或反复初始化。遇到 `Browser is not available: iab` 时只检查一次当前浏览器可用性；任务未强制 `iab` 时可按 `auto` 选择允许的执行面，任务强制 `iab` 时直接标记阻塞，不得擅自替换浏览器。
- 不执行文档未授权的批量删除、全局配置修改或其他破坏性操作。
- 遇到环境、账号、权限、前置数据或文档歧义时立即标记阻塞并返回证据，不得用猜测步骤继续执行。
- 完成后只清理本案例创建且能精确识别的数据；无法安全识别时保留数据并在结果中说明，禁止扩大删除范围。

### 13.8 阶段七：结果格式与质量门禁

每个子代理必须返回以下结构化结果：

```text
caseId: 案例编号
status: PASSED | FAILED | BLOCKED | NOT_APPLICABLE | NOT_RUN
startedAt: 开始时间
finishedAt: 完成时间
url: 实际测试地址
browser: 实际使用的浏览器技能、执行面和标签页交接状态
preconditions: 前置条件核验结果
steps: 每一步的操作和实际结果
expected: 文档预期
actual: 实际表现
console: 控制台错误或“未发现相关错误”
network: 影响结论的失败请求或“无相关失败”
evidence: 截图、日志等证据位置
createdData: 本案例创建的数据
cleanup: 已清理内容和未清理原因
issues: 缺陷或疑点
limitations: 环境限制和未验证项
```

主代理按以下门禁判定最终状态：

- `PASSED`：全部关键步骤和验收断言满足，证据完整，且没有影响结果的控制台或网络错误。
- `FAILED`：环境和前置条件满足，但实际行为与明确预期不符，并具有可复现步骤和证据。
- `BLOCKED`：因环境、账号、权限、数据、依赖失败或文档歧义无法完成验收。阻塞不能计为失败或通过。
- `NOT_APPLICABLE`：经需求、设计或测试负责人确认，该案例不适用于本次目标环境或范围，必须记录确认依据。
- `NOT_RUN`：案例尚未执行或被用户取消，必须说明原因。
- 子代理给出的状态只是初步结论；只有通过主代理门禁后才写入正式测试结果。
- 仅有成功提示、单张无上下文截图或“看起来正常”的描述，不足以判定通过。
- 子代理结论与证据矛盾时，主代理将状态暂记为 `BLOCKED` 并要求补充证据或复测。

### 13.9 失败复核、重试与缺陷归因

- 网络瞬时失败、页面未加载完成或测试服务短暂不可用可以重试一次，并同时保留首次失败与重试结果。
- 不得反复重试直到偶然通过。首次失败后重试通过的案例应记录为“不稳定”，由主代理决定是否仍需回归。
- 明确的前端失败至少由主代理复核证据；高影响或结论存疑的失败应由原执行子代理重测或安排另一子代理交叉复现。
- 复测必须使用相同前置条件和验收标准。因改变数据、账号或步骤才通过的，不得覆盖原失败结论。
- 主代理将问题归类为：前端缺陷、测试文档问题、环境阻塞、账号/权限问题、后端响应导致的前端阻塞或测试数据冲突。
- 共用后端不代表忽略后端响应。若前端因后端不可用而无法验证，应标记阻塞并说明，不得直接认定为前端失败。
- 发现缺陷时记录标题、影响范围、严重程度、复现步骤、预期、实际、证据、临时规避方式和关联案例，不在测试阶段自动修复。

### 13.10 阶段八：结果归档与最终报告

全部案例处理完成，或本次执行因明确阻塞进入阶段性收尾时，由主代理按第 9.4 节统一归档测试结果并同步关联
文档。确认所有子代理结果均已通过主代理门禁后，再生成最终报告。

最终报告至少包含：

```text
测试文档与版本
前端版本和工作区基线
发现案例数、可执行案例数和实际执行案例数
PASSED / FAILED / BLOCKED / NOT_APPLICABLE / NOT_RUN 数量
实际批次数、并发数和端口映射
缺陷清单与关联案例
不稳定案例和复测结果
未完成验收项与环境限制
文档更新清单
测试进程清理结果
2888 测试前后可用性
```

### 13.11 阶段九：环境清理与完成条件

测试完成前，主代理必须执行以下收尾检查：

- 所有由主代理为本次测试启动的 `2889` 及以上端口进程均已按 PID 精确停止；用户原有进程和 `2888` 未被停止。
- 没有遗留临时测试服务、无归属日志、下载文件或包含敏感信息的截图。
- 测试数据清理结果已逐案例记录；无法安全清理的数据已列入最终报告。
- 所有子代理结果均已收集并经过主代理门禁，没有仍在运行但未处理的测试任务。
- 关联文档状态与实际测试结果一致，没有把部分完成描述为全部通过。
- 最终向用户说明测试结果、修改的文档、发现的缺陷、未完成事项和需要人工验证的内容。

只有完成任务划分、全部可执行案例处理、结果复核、文档同步和进程清理后，主代理才能声明本次前端测试任务完成。
